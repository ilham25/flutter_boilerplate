import 'package:flutter/material.dart';

class UIKitListItem extends StatelessWidget {
  const UIKitListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Material();
  }
}
